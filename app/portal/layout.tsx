import './portal.css'

export const metadata = {
  title: 'Portal de Cliente | AUDICO ISO',
  description: 'Portal de seguimiento de procesos - AUDICO S.A.S.',
}

export default function PortalLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>
}
